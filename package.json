{
  "name": "WeatherApp",
  "private": true,
  "version": "0.0.1",
  "description": "Weather App",
  "license": "MIT",
  "devDependencies": {
    "http-server": "^0.6.1"
  },
  "scripts": {
   
    "prestart": "npm install",
    "start": "http-server -a localhost -p 8000 -c-1"

  }
}
